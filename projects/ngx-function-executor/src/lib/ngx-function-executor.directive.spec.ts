import { NgxFunctionExecutorDirective } from './ngx-function-executor.directive';

describe('NgxFunctionExecutorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFunctionExecutorDirective();
    expect(directive).toBeTruthy();
  });
});
